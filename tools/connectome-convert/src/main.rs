//! Converts raw connectome exports (neurons, synapses, cell types, transmitter
//! predictions) into the packed CSR binary that `flybrain` loads at startup.

fn main() {
    println!("connectome-convert: nothing here yet");
}
